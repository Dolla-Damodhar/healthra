# Healthra

Healthra full-stack application repository.

- `frontend/` — React + TypeScript + Vite frontend
- `backend/` — Django REST Framework backend
- `docs/` — project and deployment documentation
- `jenkins/` — Jenkins CI/CD pipeline files (to be added)
- `kubernetes/` — Kubernetes/EKS manifests (to be added)
